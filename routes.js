// 라우터 주소들 모음

// api
const LOGIN = '/login';
const REGISTER = '/register';
const LOGOUT = '/logout';
const API = '/api';


// global
const HOME = '/home';

const routes = {
    home: HOME,
    login: LOGIN,
    register: REGISTER,
    logout: LOGOUT,

    api: API
}

export default routes;